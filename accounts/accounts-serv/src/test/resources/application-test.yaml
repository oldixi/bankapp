server:
  port: 18086

spring:
  security:
    user:
      name: user
      password: user
  liquibase:
    url: jdbc:postgresql://localhost:5434/postgres
    user: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver