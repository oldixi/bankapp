apiVersion: v1
kind: Service
metadata:
  name: {{ include "bankapp-chart.fullname" . }}
  labels:
    {{- include "bankapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bankapp-chart.selectorLabels" . | nindent 4 }}
