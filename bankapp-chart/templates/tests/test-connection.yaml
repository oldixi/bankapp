apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bankapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "bankapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bankapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
