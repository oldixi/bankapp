apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash.fullname" . }}-test-connection"
  labels:
    {{- include "cash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
