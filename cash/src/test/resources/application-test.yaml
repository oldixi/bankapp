spring:
  main:
    web-application-type: servlet
  application:
    name: cash
logging:
  level:
    org.springframework.cloud.contract: debug

stubrunner:
  cloud:
    consul:
      enabled: false
  idsToServiceIds:
    ru.yandex:cash: cash

# Прямые URL для стабов
accounts:
  service:
    url: http://localhost:8082

#notifications:
  #service:
    #url: http://localhost:8085

feign:
  client:
    config:
      default:
        url: ""